{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://machine_max.io/schemas/subsystem/static/item_storage_static_attr.json",
  "title": "Item Storage Subsystem Static Attributes",
  "description": "Static attributes of the item storage container, including number of rows and columns.",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference path, used for editor intelligent suggestions and validation",
      "default": "../../docs/en_us/schemas/subsystem/subsystem_static_attr.schema.json"
    },
    "type": {
      "type": "string",
      "const": "machine_max:item_storage",
      "description": "Subsystem type, must be 'machine_max:item_storage'."
    },
    "basic_durability": {
      "type": "number",
      "minimum": 0,
      "default": 20.0,
      "description": "Subsystem basic durability, becomes paralyzed when reduced to 0, restores functionality when repaired to 30% durability."
    },
    "rows": {
      "type": "integer",
      "minimum": 1,
      "default": 3,
      "description": "Number of rows of item slots in the container."
    },
    "columns": {
      "type": "integer",
      "minimum": 1,
      "default": 9,
      "description": "Number of columns of item slots in the container."
    }
  },
  "required": ["type"],
  "additionalProperties": false,
  "examples": [
    {
      "type": "machine_max:item_storage",
      "basic_durability": 100.0,
      "rows": 6,
      "columns": 9
    }
  ]
}